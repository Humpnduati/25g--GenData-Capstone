1.circuits table

Issues:

- Mix of string and float for coordinates (lat, lng, alt).

- alt has missing values.

Power Query Transformations:

- Convert lat, lng, and alt → Decimal Numbers.

- Replace null alt with 0 or a descriptive placeholder (“Unknown”).

- Clean name and location to remove extra spaces.

2. constructors table

Issues:

- Name and Nationality are text but may have inconsistent capitalization.

Power Query Transformations:

- Apply Text.Proper() to name and nationality.

- Ensure constructorId stays numeric.

3. drivers table

Issues:

- dob is text, should be Date.

- Code and Number contain nulls.

- Names (forename, surname) have inconsistent casing.

Power Query Transformations:

- Convert dob → Date type.

- Standardize forename and surname to Proper Case.

- Replace nulls in number with “N/A”.

4. status table

Issues: Only statusId and status (e.g., “Finished”, “Accident”).

Power Query Fix: No major changes, but ensure status is Categorical. 

5. races table

Issues:

- date and time stored separately.

- Missing values in fp1_date, fp2_date, fp3_date, quali_date, sprint_date.

Power Query Transformations:

- Merge date + time into a single DateTime column.

- Replace nulls in free practice dates with a placeholder.

6. results / sprint_results / qualifying

Issues:

- Some numeric columns (position, points) contain nulls.

- rank should be integer but contains nulls.

- Times are stored as text.

Power Query Transformations:

- Convert time/milliseconds into Duration format.

- Replace null position with “DNF” or “N/A”.

- Ensure all IDs (raceId, driverId, etc.) are numeric.

7. pit_stops

Issues:

- duration stored as text (23.456).

- time is race timestamp, but stored as text.

Power Query Fix:

- Convert duration → Decimal Number.

- Parse time into a proper Time/Duration.

8. lap_times

Issues:

- time stored as text (e.g., 1:23.456).

Power Query Transformations:

- Split into minutes, seconds, milliseconds.

- Convert to Duration type for easy lap comparisons.

9. driver_standings / constructor_standings

Issues:

- points, wins fine but positionText mixes numbers and text (1, R).

Power Query Fix:

- Create two fields:

- position (numeric)

- positionText (original)